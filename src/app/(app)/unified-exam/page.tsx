import { UnifiedExam } from "@/components/unified-exam";

// The component renders its own header with the progress bar, so the page is
// just a container.
export default function UnifiedExamPage() {
  return <UnifiedExam />;
}
